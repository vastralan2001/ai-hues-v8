# 研发排期建议：静态 vs 动态分工

> 整理时间：2026-06-01
> 原则：**前端能自己解决的绝不麻烦后端，后端只负责必须服务端存储/计算的数据**

---

## ✅ 第一梯队：前端静态处理（本周就能做完，不需要后端排期）

### 1. 侧边栏 / Footer 数字动态化
**现状**：左侧边栏和 footer 写死了 `57 tools + 3 games`  
**做法**：前端调用已有的 `safeListTools({ pageSize: 100 })` 和 `safeListGames`，用 `.length` 动态渲染  
**文件**：`dict.ts`（需要改文案模板）、Sidebar 组件、footer 区域  
**工作量**：0.5 天  
**风险**：无，API 已存在

---

### 2. 博客元数据统一
**现状**：博客列表、详情页、RSS 三处各自硬编码同一套文章元数据  
**做法**：把 9 篇文章的 `slug/title/excerpt/date/tag` 统一抽到一个 `blog-meta.json`，三处都读这个文件  
**工作量**：0.5 天  
**风险**：无，纯前端重构

---

### 3. 分类数量自动计算
**现状**：`HOME_CATEGORIES` 里写了 `count: 0`（我已改成 0），但 API 正常时前端已经会动态算  
**做法**：确认 `categoryCounts` 逻辑无误即可，无需额外改动  
**工作量**：已做完 ✅

---

### 4. Popular Tools 评分处理
**现状**：`POPULAR_HIGHLIGHTS` 里的 `metrics` 已改成 `—` 占位  
**做法**：上线前确认——是隐藏评分区域，还是保留 `—` 占位等后端 analytics ready  
**工作量**：5 分钟决策

---

## 🔵 第二梯队：后端已有字段，只需补数据（1 天，后端同学顺手做）

### 5. 工具定价与 Credit 消耗
**现状**：`catalog-api.ts` 里 `priceTag` 和 `creditCost` 字段已有，但 `CatalogCards.tsx` 注释写 `backend has no price field yet`  
**需要确认**：
- 后端数据库里 `tools` 表是否真的有 `price_tag` / `credit_cost` 字段？
- 如果有，补一下这 70+ 工具的定价数据即可
- 如果没有，加字段 + 补数据

**工作量**：
- 已有字段：半天补数据
- 无字段：1 天（加字段 + migration + 补数据）

**优先级**：高（影响首页工具卡片展示）

---

## 🔴 第三梯队：必须后端开发（需要正式排期，建议分两周）

### Week 1：用户核心资产（优先级最高，用户一注册就会看到）

| 功能 | 为什么必须后端 | 接口建议 | 预估工作量 |
|---|---|---|---|
| **用户积分余额** | 多设备同步、防篡改 | `GET /api/v1/user/credits` | 1 天 |
| **积分流水记录** | 用户需要查账 | `GET /api/v1/user/credits/transactions` | 0.5 天 |
| **每日签到** | 服务端判断今天是否已签到 | `POST /api/v1/user/checkin` | 0.5 天 |
| **游戏积分结算** | 玩游戏赚积分、用工具扣积分 | `POST /api/v1/games/{slug}/reward` | 1 天 |

**Week 1 合计**：3 天，1 个后端同学一周可以做完

---

### Week 2：排行榜 + 许愿板（不影响核心流程，但影响留存）

| 功能 | 为什么必须后端 | 接口建议 | 预估工作量 |
|---|---|---|---|
| **游戏排行榜** | 全局排名、持久化 | `GET /api/v1/games/{slug}/leaderboard` | 1 天 |
| **工具使用统计** | 统计每个工具被用了多少次 | 埋点或调用计数 | 1 天 |
| **Ranking 页面数据** | 工具排名 + 个人排名 | `GET /api/v1/analytics/rankings` | 1 天 |
| **许愿板投票** | 多人投票、防刷票 | `POST /api/v1/wishlist/{id}/vote` | 1 天 |
| **成就系统** | 用户达成条件后解锁 | `GET /api/v1/user/achievements` | 1 天 |

**Week 2 合计**：5 天，可以拆给 2 个后端同学并行

---

## 🟡 第四梯队：可以放到上线后再迭代（不影响 MVP）

| 功能 | 说明 | 建议上线后第几周做 |
|---|---|---|
| **支付系统** | Pricing 页面目前只是展示，没有支付按钮 | 第 3-4 周 |
| **Team 工作区** | Team 计划里的共享 workspace | 第 4-6 周 |
| **Admin 后台** | 管理工具定价、上下架、看数据 | 第 6-8 周 |
| **左侧分类树状导航** | PLAN.md 里提到的低优先级功能 | 第 8 周以后 |

---

## 📋 给研发老师的最小排期表

### 前端同学（你或前端负责人）

| 时间 | 任务 |
|---|---|
| 本周内 | 侧边栏/Footer 数字动态化、博客元数据统一 |
| 等后端定价字段 ready | 移除 `CatalogCards.tsx` 里的 `TOOL_PRICING` 硬编码 |
| 等后端积分 API ready | 把 `CreditDisplay`/`GameCreditBadge` 从 localStorage 切到 API |
| 等后端排行榜 API ready | Ranking 页面从 "Coming Soon" 切换到真实数据展示 |

### 后端同学

| 时间 | 任务 | 产出 |
|---|---|---|
| **Week 1** | 用户积分系统（余额+签到+流水） | 3 个 API |
| **Week 2 上半** | 工具定价字段补全 + 游戏积分结算 | DB 数据补齐 + 2 个 API |
| **Week 2 下半** | 排行榜（3 个游戏 + 工具排名） | 2 个 API |
| **Week 3** | 许愿板投票 + 成就系统 | 3 个 API |

---

## 💡 关键决策点（需要你拍板）

1. **Pricing 页面上的价格（Free/Pro $9/Team $29）是最终确定的吗？**
   - 如果是：保留静态，后端只需要在上线前确认积分配额规则
   - 如果不是： Pricing 页面先隐藏或标 "Beta 定价"

2. **用户体系怎么做？**
   - 方案 A：先匿名（用设备 ID + localStorage），不上注册登录，积分存在本地（最快上线）
   - 方案 B：直接上手机号/邮箱注册，积分存服务端（需要额外 3-5 天做 auth）

3. **排行榜范围？**
   - 全局排行榜（所有人一起排）→ 需要后端
   - 仅个人历史记录（只显示自己的最高分）→ 前端 localStorage 就能做，可以先替代

---

## ✅ 总结：推荐的最小上线路径

**MVP 上线 = 前端静态清理（本周） + 后端 Week 1（积分系统） + 后端 Week 2 上半（定价补全）**

这样 2 周内可以有一个「能看、能用、积分系统真实、不展示假数据」的版本上线。

排行榜、许愿板、成就可以作为上线后第一次迭代的亮点功能发布。
