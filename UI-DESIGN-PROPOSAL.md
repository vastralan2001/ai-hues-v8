# AIHues UI  redesign — 三版方案（对标 Claude 风格）

> 核心方向：温暖、书卷气、不荧光、低饱和、大量留白

---

## 一、Claude 设计系统提炼

| Token | 值 | 用途 |
|---|---|---|
| **主色** | `#d97757` | 暖珊瑚橙，CTA、高亮、图标 |
| **深色** | `#1a1a19` | 标题文字（非纯黑，偏暖黑） |
| **正文** | `#3d3d3a` | 正文文字（深暖灰） |
| **次要文字** | `#8a8880` | 辅助说明、meta |
| **背景** | `#faf9f5` | 米白/奶油色（不是纯白 `#fff`） |
| **卡片背景** | `#f5f3ee` | 卡片、区块底色 |
| **边框** | `#e8e6dc` | 极浅暖灰边框 |
| **深蓝灰** | `#6a9bcc` | 辅助强调色（按钮次态、链接） |
| **柔和绿** | `#788c5d` | 第三种点缀（标签、成功态） |

**字体：**
- 标题：`Poppins`（现代几何无衬线，6/700/800 字重）
- 正文：`Lora`（优雅衬线，400/500 字重）
- 代码/标签：`JetBrains Mono` 或系统等宽

**禁止：**
- ❌ 荧光紫/蓝/绿渐变
- ❌ 纯白背景（必须用米白）
- ❌ Inter/Roboto/Arial
- ❌ 强对比渐变（如 `#b45309 → #d97706` 这种）

---

## 二、三版方案

### 方案 A：「Claude 同款」—— 温暖编辑部

**风格关键词：** 书卷气、杂志排版、不对称、大量留白

**配色：** 完全对标 Claude
```css
:root {
  --bg: #faf9f5;
  --bg-card: #f5f3ee;
  --bg-header: rgba(250, 249, 245, 0.92);
  --text-primary: #1a1a19;
  --text-secondary: #8a8880;
  --accent: #d97757;
  --accent-hover: #c46a4a;
  --accent-soft: rgba(217, 119, 87, 0.08);
  --border: #e8e6dc;
  --accent-blue: #6a9bcc;
  --accent-green: #788c5d;
}
```

**渐变规则：**
- 唯一渐变：`linear-gradient(135deg, #d97757 0%, #c46a4a 100%)` — 仅用于 CTA 按钮，极其克制
- 无其他渐变、无发光、无阴影扩散

**Hero 方案（3 选）：**

**A1 — 数据驱动型：**
```
主标题：57 个 AI 工具，每一个都经过真实测试
副标题：从文案生成到代码调试，从数据分析到设计辅助。
        没有花里胡哨的演示，只有能用的结果。
CTA：[开始探索]  [看评测 →]
底部 micro：已服务 12,000+ 创作者 · 平均评分 4.7
```

**A2 — 场景代入型：**
```
主标题：写不出文案？调不好代码？找不到合适的 AI 工具？
副标题：我们把真正好用的 AI 工具筛出来，按场景分类，
        附上实测评分和使用指南。不用大海捞针。
CTA：[找工具]  [看工作流程 →]
底部 micro：覆盖营销、开发、设计、运营 4 大场景
```

**A3 — 极简主张型：**
```
主标题：AI 工具太多，时间太少
副标题：AIHues 帮你过滤噪音。57 个精选工具，
        每个都有真人实测、场景评分和保姆级教程。
CTA：[免费使用]
底部 micro：无需注册 · 中英文双语 · 每日更新
```

**卡片样式：**
- 圆角：`16px`（比现在的 14px 更柔和）
- 边框：`1px solid var(--border)`
- 背景：`var(--bg-card)`
- 阴影：无（或极淡的 `0 1px 2px rgba(26,26,25,0.04)`）
- Hover：边框颜色变 `--accent`，卡片微上移 `-2px`

**导航栏：**
- 背景：`rgba(250, 249, 245, 0.92)` + `backdrop-filter: blur(16px)`
- 文字：`#1a1a19`，`14px`，`500`
- 底部边框：`1px solid var(--border)`

---

### 方案 B：「纸与墨」—— 杂志印刷感

**风格关键词：** 印刷品、衬线标题主导、粗体排版、网格感

**配色：** 在 Claude 基础上加入更多纸质纹理感
```css
:root {
  --bg: #f7f5f0;           /* 稍暖的纸色 */
  --bg-card: #f0ede6;      /* 卡片像便签纸 */
  --text-primary: #1c1c1a;
  --text-secondary: #7a7870;
  --accent: #c45a3a;       /* 比方案 A 深一点的砖红 */
  --accent-hover: #a84d32;
  --accent-soft: rgba(196, 90, 58, 0.06);
  --border: #dddad0;
  --ink: #2c2c29;          /* 墨黑，用于大标题 */
}
```

**字体变更：**
- 大标题：`Playfair Display`（衬线，更有印刷品气质）
- 小标题/正文：`Lora`
- 标签/按钮：`Poppins`（小号、全大写、字间距加宽）

**排版特征：**
- Hero 区：左对齐大标题，`clamp(2.5rem, 5vw, 4rem)`
- 标题下方加一条 `2px` 的 accent 色短线装饰
- 正文 `max-width: 640px`，行高 `1.7`
- 工具卡片：图标区域用 accent 色填充圆形，白色图标

**Hero 方案：**

**B1 — 编辑口吻：**
```
主标题：AI Tools, Reviewed.
副标题：We tested 57 AI tools so you don't have to.
        Each one comes with a real-world score,
        use-case guide, and honest verdict.
CTA：[Browse the catalog]  [Read reviews →]
底部：Scroll ↓
```

**B2 — 中文编辑口吻：**
```
主标题：57 个 AI 工具，逐个实测
副标题：不跟风、不软文。每一个工具都经过真实场景测试，
        附带评分、教程和替代方案。帮你省掉试错时间。
CTA：[浏览工具]  [看评测 →]
底部：向下滚动 ↓
```

**B3 — 对话式：**
```
主标题：你试了多少个 AI 工具才找到一个能用的？
副标题：我们替你试了 57 个。留下了真正好用的，
        写好了使用指南，标上了诚实评分。
CTA：[直接开始用]
底部：无需登录 · 没有 paywall
```

**特殊处理：**
- 分类标签：小圆角 pill + 背景色块，不用边框
- 价格标签：`FREE` / `FREEMIUM` / `PAID` 全大写，小字号，粗体
- 引用/评测区：左侧 `3px` accent 色竖线 + 衬线斜体

---

### 方案 C：「数字工匠」—— 克制的技术感

**风格关键词：** 瑞士排版、网格系统、单色调+单点缀色、精确

**配色：** 更冷峻，但仍有温度
```css
:root {
  --bg: #f5f4f0;           /* 偏冷的灰白 */
  --bg-card: #eae9e4;      /* 卡片 */
  --text-primary: #181816;  /* 极深暖黑 */
  --text-secondary: #6b6960;
  --accent: #d97757;        /* 保留珊瑚橙，但用得极少 */
  --accent-hover: #c46a4a;
  --accent-soft: rgba(217, 119, 87, 0.05);
  --border: #dcdad0;
  --grid: #e8e6dc;          /* 网格线颜色 */
}
```

**字体：**
- 全部标题+正文：`Space Grotesk`（几何无衬线，比 Poppins 更冷峻）
- 或标题 `DM Sans` + 正文 `Source Serif 4`
- 数字/评分：`JetBrains Mono`

**布局特征：**
- 严格的网格系统，卡片等宽等高
- 大量留白，信息密度低
- 分割线用 `1px solid var(--border)` 代替卡片边框
- 评分数字用大号等宽字体突出显示

**Hero 方案：**

**C1 — 极简直给：**
```
主标题：57 tools. Real tests. Honest scores.
副标题：No demos. No fluff. Just AI tools that actually work,
        with step-by-step guides and candid reviews.
CTA：[Explore]  [See rankings →]
底部：12K+ users · 4.7 avg rating · Updated daily
```

**C2 — 中文极简：**
```
主标题：57 个 AI 工具 · 实测评分 · 诚实推荐
副标题：没有包装过的演示视频，没有付费软文。
        只有真实测试结果、使用教程和替代方案。
CTA：[开始探索]  [查看排名 →]
底部：12,000+ 用户正在使用
```

**C3 — 反问式：**
```
主标题：还在一个个试用 AI 工具？
副标题：我们替你试完了。57 个工具，每个都经过 6 维度评分，
        附带保姆级教程。找到合适的，直接开始用。
CTA：[免费使用]
底部：无需注册 · 无付费墙
```

**特殊处理：**
- 工具卡片：无圆角（`0px` 或 `2px`），纯边框区分
- 评分：大号等宽数字 `4.7` + 小号 `/5.0`
- 标签：直角矩形，背景色块
- Hover：背景色微变，无阴影

---

## 三、全局改动清单（三版通用）

| 组件 | 当前状态 | 改后 |
|---|---|---|
| **背景色** | `#ffffff` / `#0c0a09` | `#faf9f5` / `#1a1a19` |
| **卡片背景** | `bg-white` / `dark:bg-[#1c1917]` | `#f5f3ee` / `#252422` |
| **边框** | `border-border`（硬灰） | `#e8e6dc`（暖灰） |
| **主按钮** | 琥珀渐变 `135deg, #b45309, #d97706` | 纯色 `#d97757`，hover `#c46a4a` |
| **次按钮** | 边框+文字 | 边框 `#1a1a19` + 文字 `#1a1a19` |
| **导航栏** | `bg-white/[0.92]` | `rgba(250,249,245,0.92)` + blur |
| **图标** | 彩色 emoji | 统一用 `#d97757` 单色图标或线条图标 |
| **渐变** | 多处强对比渐变 | 仅 CTA 一个微妙渐变，其余纯色 |
| **阴影** | `shadow-[0_4px_12px_rgba(180,83,9,0.12)]` | 无阴影或极淡 `rgba(26,26,25,0.04)` |
| **字体** | Inter / system | Poppins + Lora / Playfair + Lora / Space Grotesk |

---

## 四、Hero 文案汇总（9 选）

| 编号 | 风格 | 主标题 | 副标题 |
|---|---|---|---|
| A1 | 数据驱动 | 57 个 AI 工具，每一个都经过真实测试 | 从文案生成到代码调试……只有能用的结果 |
| A2 | 场景代入 | 写不出文案？调不好代码？找不到合适的 AI 工具？ | 我们把真正好用的筛出来……不用大海捞针 |
| A3 | 极简主张 | AI 工具太多，时间太少 | AIHues 帮你过滤噪音…… |
| B1 | 编辑口吻(EN) | AI Tools, Reviewed. | We tested 57 AI tools so you don't have to…… |
| B2 | 编辑口吻(ZH) | 57 个 AI 工具，逐个实测 | 不跟风、不软文……帮你省掉试错时间 |
| B3 | 对话式 | 你试了多少个 AI 工具才找到一个能用的？ | 我们替你试了 57 个…… |
| C1 | 极简(EN) | 57 tools. Real tests. Honest scores. | No demos. No fluff…… |
| C2 | 极简(ZH) | 57 个 AI 工具 · 实测评分 · 诚实推荐 | 没有包装过的演示视频…… |
| C3 | 反问式 | 还在一个个试用 AI 工具？ | 我们替你试完了…… |

---

## 五、下一步

1. **你选：** 方案 A / B / C（或混合）
2. **你选：** Hero 文案编号（或自己改）
3. **我执行：** 改 `globals.css` + `tailwind.config` + `page.tsx` + `SiteChrome.tsx`
4. **预计工时：** 4-6 小时（全局 token + Hero + 卡片 + 按钮 + 导航）

请回复：「方案 X + 文案 Y」 即可开干。
