workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS
      when: never
    - if: $CI_COMMIT_BRANCH
    - if: $CI_COMMIT_TAG

variables:
  CI_REGISTRY: 'msai-cn-beijing.cr.volces.com/msai'
  GIT_DEPTH: "0"
  GIT_CLEAN_FLAGS: -ffdx

default:
  image: msai-cn-beijing.cr.volces.com/msai/devops/ci-base-node:v0.0.2
  tags:
    - kimi
  before_script:
    - pnpm install --frozen-lockfile

stages:
  - test
  - build

lint:
  stage: test
  script:
    - moon run :lint :format-check :typecheck
    - echo "✅ Check successful"

Build Web Image:
  stage: build
  script:
    - moon run papers-web:container
    - echo "✅ Build web image successful"
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH

Build API Image:
  stage: build
  script:
    - moon run papers-api:container
    - echo "✅ Build api image successful"
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
